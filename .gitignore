# Python / Django
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/

# SQLite database (optional, omit if database file shouldn't be tracked)
*.sqlite3

# Kiosk klientlaridan yuklangan skrinshotlar (runtime-generated)
server/media/

# Klient kiosk log fayli (har bir PC'da mahalliy, runtime-generated)
client/client_locker.log

# Mijoz "Kabinet" holatini tiklash uchun mahalliy sessiya fayli
# (har bir PC'da mahalliy, runtime-generated)
client/session_state.json

# Har bir PC'ga xos sozlamalar (PC nomi, server manzili, o'sha PC'dagi
# o'yinlar ro'yxati) — bularni git kuzatishi HAR BIR "git pull"da
# to'qnashuvga sabab bo'lardi (45+ marta shu muammo takrorlandi).
# Namuna uchun client/config.example.json va
# client/regenerate_config.example.py tracked holda qoladi.
client/config.json
client/regenerate_config.py

# macOS
.DS_Store

# IDEs
.idea/
.vscode/
*.swp
*.swo
